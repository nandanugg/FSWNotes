function functionName(paramater) {
    const process = "this is a process"
}
let oldName = "Budi"
let newName = oldName
// meretrun = nilai balik , mengembalikan isi dari value

let amount = addDolarSign('300')
function addDolarSign(number) {
    const output = `$${number}`
    return output
}
console.log("amount", amount);